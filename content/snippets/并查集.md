---
id: 并查集
title: 并查集
language: C++
tags:
  - 并查集
  - 数据结构
description: 并查集，支持记录连通块大小，在近似log的复杂度内查询
created_at: '2026-06-04 18:49:49'
updated_at: '2026-06-04 18:50:54'
---
```C++
struct DSU
{
    vector<int> f, siz;
    int cnt;

    DSU(int n) : f(n), siz(n, 1), cnt(n)
    {
        iota(f.begin(), f.end(), 0);
    }
    // DSU(int n) : fa(n + 1), sz(n + 1, 1), count(n) 
    // {
    //     std::iota(fa.begin(), fa.end(), 0);
    // }

    int find(int x)
    {
        while (x != f[x])
            x = f[x] = f[f[x]];
        return x;
    }

    bool merge(int x, int y)
    {
        x = find(x);
        y = find(y);
        if (x == y)
            return false;
        // if (siz[x] < siz[y]) swap(x, y);
        siz[x] += siz[y];
        f[y] = x;
        cnt--;
        return true;
    }

    bool same(int x, int y) { return find(x) == find(y); }
    int size(int x) { return siz[find(x)]; }
};
```
